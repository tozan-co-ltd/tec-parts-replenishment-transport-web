@{
    ViewData["Title"] = "在庫調整";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card" id="inventory-information-page">
    <div class="row table-content inventoryInformationContent">
        <div style="padding: 20px;">
            <button type="button" class="btn btnBack" onclick="history.back()">
                <i class="fa-solid fa-caret-left"></i> 戻る
            </button>
            <div class="d-flex justify-content-center">
                <table id="tblInventoryInformation" class="table table-striped table-dark">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>品番</th>
                            <th>現在数量</th>
                            <th>現在-追加数</th>
                            <th class="registerTh">登録</th>
                        </tr>
                    </thead>
                    <tbody id="express-table-row-left">
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Error messages -->
    <div class="noneDateMess text-center text-danger" style="display: none;">
        E2007 該当データがありません。
    </div>
    <div class="connectionReplenishmentError text-center text-danger" style="display: none;">
        E3003 ネットワークに接続できませんでした。
    </div>

    <!-- Loading overlay -->
    <div id="overlay">
        <div class="cv-spinner">
            <span class="dots-circle-spinner loading"></span>
        </div>
    </div>
</div>

<script>
    document.querySelectorAll('.btnRegister').forEach(btn => {
      btn.addEventListener('click', function () {
                Swal.fire({
                    title: `開始してもよろしいですか？`,
                    icon: 'warning',
                    showCancelButton: true,
                    confirmButtonColor: '#0d6efd',
                    cancelButtonText: 'いいえ',
                    confirmButtonText: 'はい',
                    allowOutsideClick: false,
                }).then((result) => {
                    if (result.isConfirmed) {
                        console.log('Confirmed - 登録処理を実行');
                    }
                });
            });
        });

    document.querySelectorAll('.btnclose').forEach(btn => {
        btn.addEventListener('click', function () {
                      Swal.fire({
                          title: 'この品番を取消しますか？',
                          icon: 'question',
                          showCancelButton: true,
                          confirmButtonColor: '#dc3545',
                          cancelButtonText: 'いいえ',
                          confirmButtonText: 'はい',
                          allowOutsideClick: false,
                      }).then((result) => {
                          if (result.isConfirmed) {
                              this.closest('tr').remove();
                          }
                      });
                  });
    });
</script>

